# a mybinder.org-ready environment for demoing etc_jupyterlab_nbgrader_validate
# this environment may also be used locally on Linux/MacOS/Windows, e.g.
#
#   conda env update --file binder/environment.yml
#   conda activate etc-jupyterlab-nbgrader-validate-demo
#
name: etc-jupyterlab-nbgrader-validate-demo

channels:
  - conda-forge

dependencies:
  # runtime dependencies
  - python >=3.10,<3.11.0a0
  - jupyterlab >=4.0.0,<5
  # labextension build dependencies
  - nodejs >=18,<19
  - pip
  - wheel
  # additional packages for demos
  # - ipywidgets
